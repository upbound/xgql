{
  "license": "Apache-2.0",
  "devDependencies": {
    "graphql-schema-linter": "^2.0.1",
    "prettier": "2.2.1"
  },
  "scripts": {
    "format": "yarn run prettier --write ../schema/*",
    "lint": "yarn run graphql-schema-linter",
    "build": "true",
    "test-ci": "true"
  }
}